from django.test import TestCase
#from .models import Tag

# Need to fix this test
# class TagModelTests(TestCase):
#     def duplicate_tag_case(self):
#         name = 'test'
#         test = Tag.objects.create(name=name)
#         test.save()
#         dup = Tag.objects.create(name=name)
#         dup.save()
#         self.assertEqual(test.name, dup.name)
